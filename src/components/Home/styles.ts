import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        marginTop: 25,
        padding: 10,
        flex: 1
    },
    //TODO fix this hack
    routes: {
        minHeight: '85%',
        maxHeight: '85%'
    }
});