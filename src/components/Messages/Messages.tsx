import * as React from 'react';
import { View, Text } from 'react-native';

import styles from './styles';

const Messages = (props: {}) => (
  <View style={styles.container}>
    <Text>In Messages Component</Text>
  </View>
);
export default Messages;
