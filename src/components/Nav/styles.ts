import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    nav: {
        flexDirection: 'row',
        justifyContent: 'space-around',
        bottom: 0
    },
    navItem: {
        flex: 1,
        alignItems: 'center',
        padding: 10,
    },
    subNavItem: {
        padding: 5,
    },
})